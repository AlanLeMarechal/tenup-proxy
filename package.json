{
  "name": "tenup-proxy",
  "version": "2.0.0",
  "description": "Proxy REST pour l'API publique TenUp FFT",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "engines": {
    "node": ">=18"
  }
}
